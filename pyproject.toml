# This is a documentation project, not a Python package
# Dependencies are listed in requirements.txt and installed via uv pip install

[tool.uv]
# uv configuration for this project

[tool.sphinx]
# Sphinx build configuration hints
source-dir = "."
build-dir = "_build"
